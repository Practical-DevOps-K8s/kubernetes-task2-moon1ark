apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15
          env:
            - name: POSTGRES_DB
              value: rails_db
            - name: POSTGRES_USER
              value: rails_user
            - name: POSTGRES_PASSWORD
              value: rails_password
          ports:
            - containerPort: 5432
